%% -*- mode: erlang; -*-

{application, smtp,
 [
  {description, "An erlang SMTP server/client framework"},
  {vsn, "0.1"},
  {modules,
   [
    binstr,
    smtp_client,
    smtp_server,
    smtp_server_session,
    mimemail,
    smtp_util,
    socket,
    smtp_server_example
   ]
  },
  {applications, [kernel, stdlib]},
  {registered, []}
 ]
}.
